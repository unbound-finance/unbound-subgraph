{
  "name": "und_test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ unbound-finance/unbound-subgraph",
    "create-local": "graph create --node http://localhost:8020/ unbound-finance/unbound-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ unbound-finance/unbound-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 unbound-finance/unbound-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
